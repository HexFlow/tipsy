void helper(int a) {
    a = 2;
}

int main() {
    int avar;
    int somevar = 2 + sort(coollist);
    sort(list);
}

void merge(int c) {
    helper(d);
}

int sort(char a) {
    b = 2;
    sort(a/2);
    sort(a - a/2);
    *b = 2;
    merge(a, b);
}
